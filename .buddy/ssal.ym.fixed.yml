- pipeline: ssal_ym
  name: ssal.ym
  refs:
  - refs/heads/main
  events:
  - type: PUSH
  fail_on_prepare_env_warning: true
  actions:
  - action: gh auth status
    type: GIT_HUB_CLI
    integration: alexs12345x
    execute_commands:
    - gh auth status
    shell: BASH
